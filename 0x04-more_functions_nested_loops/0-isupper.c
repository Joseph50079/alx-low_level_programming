#include "main.h"

/**
 * _isupper - function prototype
 * description - return 1 if c is upper
 * return - 0 or 1 if otherwise
 */

int _isupper(int c);
{
	int c;

	c = "97";

	for (c = "97")
	{
		if (c = "97"; c <= "122"; c++)
		{
		return(0);
		}

		else if (c = "65"; c <= "96"; c++)
		{ 
		return (1);
		}

	}

	return (0);
}
