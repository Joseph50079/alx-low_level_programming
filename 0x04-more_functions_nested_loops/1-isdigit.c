#include "main.h"
/**
 * _isdigit - function
 * @c: return 1 if it's a digit
 * description - 1 if it is a digit
 * Return: 0
 */

int _isdigit(int c);
{
	int c;

	for (c = "45"; c <= "54";)
		return (1);
	else
		return (0);
}
